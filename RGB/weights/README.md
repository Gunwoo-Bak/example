# Model Weights

Place model checkpoints here, or pass `--checkpoint` with an absolute path.

Known local checkpoint from the source workspace:

```text
/home/gunwoo/CrackSegmentation/outputs/dachung_finetune/best.pt
```

The checkpoint is about 175 MB, so it is intentionally not committed to Git.
Use Git LFS or GitHub Releases if this repository needs to distribute weights.

